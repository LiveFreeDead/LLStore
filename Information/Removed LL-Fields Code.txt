F = GetFolderItem(Slash(AppPath)+"Tools/LL-Fields.ini", FolderItem.PathTypeShell)

If F.Exists Then
  Var inputStream As TextInputStream
  inputStream = TextInputStream.Open(F)
  // Step 3: Read Each Line
  Var line As String
  RL = ""
  While Not inputStream.EndOfFile
    line = inputStream.ReadLine
    // Process each line (for demonstration purposes, we'll just output each line in a MessageBox)
    RL = line
  Wend
  
  // Step 4: Close the Stream
  inputStream.Close
End If

If RL = "" Then RL = "RefID Selected BuildType Compressed Hidden ShowAlways ShowSetupOnly Installed Arch OS TitleName Version Categories Description URL Priority PathApp PathINI FileINI FileCompressed FileIcon IconRef FileScreenshot FileFader FileMovie Flags Tags Publisher Language Rating Additional Players License ReleaseVersion ReleaseDate RequiredRuntimes Builder InstalledSize LnkTitle LnkComment LnkDescription LnkCategories LnkRunPath LnkExec LnkArguments LnkFlags LnkAssociations LnkTerminal LnkMultiple LnkParentRef LnkIcon NoInstall DesktopEnv UniqueName Dependencies "

